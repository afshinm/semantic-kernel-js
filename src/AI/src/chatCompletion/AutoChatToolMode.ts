import { ChatToolModeBase } from './ChatToolModeBase';

export class AutoChatToolMode extends ChatToolModeBase {
  // eslint-disable-next-line @typescript-eslint/no-useless-constructor
  constructor() {
    super();
  }
}
